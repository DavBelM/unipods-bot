# Python
__pycache__/
*.pyc

# venv (use ./unipods locally; recreate with requirements.txt elsewhere)
unipods/

# local vector index (regenerated by build_index.py / the Supabase migration script)
unipods_db/

# private / large source data — not meant for the repo
chat.txt
chat.md
*.mp4

# secrets — never commit real keys
.env
openai.txt
zapier.txt

# whatsapp_bot.py session credentials — never commit
neonize.db
*.db
.vercel
.env*
